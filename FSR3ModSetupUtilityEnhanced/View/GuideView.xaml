<UserControl
    x:Class="FSR3ModSetupUtilityEnhanced.View.GuideView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:FSR3ModSetupUtilityEnhanced.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:FSR3ModSetupUtilityEnhanced.ViewModel"
    d:DesignHeight="600"
    d:DesignWidth="940"
    Foreground="WhiteSmoke"
    mc:Ignorable="d">

    <UserControl.DataContext>
        <viewmodels:GuideViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>
        <Style x:Key="TopicsListBoxStyle" TargetType="ListBox">
            <Setter Property="Background" Value="#333333" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="WhiteSmoke" />
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border
                            Name="Border"
                            Padding="{TemplateBinding Padding}"
                            Background="Transparent"
                            SnapsToDevicePixels="true">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="#007ACC" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="#444444" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border
            Grid.Column="0"
            Background="#333333"
            CornerRadius="5">
            <ListBox
                DisplayMemberPath="Title"
                ItemsSource="{Binding Topics}"
                SelectedItem="{Binding SelectedTopic, Mode=TwoWay}"
                Style="{StaticResource TopicsListBoxStyle}" />
        </Border>

        <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
            <StackPanel DataContext="{Binding SelectedTopic}">

                <TextBlock
                    FontSize="36"
                    FontWeight="Bold"
                    Text="{Binding Title}"
                    TextWrapping="Wrap" />

                <Border
                    Height="300"
                    Margin="0,20,0,20"
                    Background="#2D2D30"
                    CornerRadius="5">
                    <Image Source="{Binding ImagePath}" Stretch="Uniform" />
                </Border>

                <TextBlock
                    FontSize="16"
                    LineHeight="24"
                    Text="{Binding Description}"
                    TextWrapping="Wrap" />

                <Button
                    Margin="0,20,0,0"
                    Padding="20,10"
                    HorizontalAlignment="Left"
                    Content="Watch Video Guide"
                    FontSize="16">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#007ACC" />
                            <Setter Property="Foreground" Value="White" />
                            <Setter Property="Cursor" Value="Hand" />
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding VideoUrl}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>